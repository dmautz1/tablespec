column:
  data_type: DECIMAL
  description: Total pharmacy paid amount.
  exclude_from_change_detection: false
  name: total_rx_paid
  nullable:
    default: true
  precision: 18
  preserve_literal_null: false
  scale: 2
derivation:
  candidates:
    - column: paid_amt
      expression: SUM(paid_amt)
      priority: 1
      table: rx_claims
  survivorship:
    explanation: Total pharmacy paid dollars per member.
    strategy: single_source
