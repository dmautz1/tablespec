column:
  data_type: DECIMAL
  description: Total medical paid amount.
  exclude_from_change_detection: false
  name: total_med_paid
  nullable:
    default: true
  precision: 18
  preserve_literal_null: false
  scale: 2
derivation:
  candidates:
    - column: paid_amt
      expression: SUM(paid_amt)
      priority: 1
      table: med_claims
  survivorship:
    explanation: Total medical paid dollars per member.
    strategy: single_source
