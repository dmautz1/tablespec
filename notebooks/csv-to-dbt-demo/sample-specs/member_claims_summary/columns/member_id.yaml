column:
  data_type: VARCHAR
  description: Universe key -- every member seen in any feed.
  exclude_from_change_detection: false
  length: 10
  name: member_id
  nullable:
    default: false
  preserve_literal_null: false
derivation:
  strategy: primary_key
  survivorship:
    explanation: Primary key passthrough from the member universe.
    strategy: single_source
